@using Event.Data.Objects.Entities
@using MyEventPlan.Data.Service.Enum
@model AppUser
@{
    ViewBag.Title = "View";
    ViewBag.Header = Model.DisplayName;
    Layout = "../Shared/_LayoutForm.cshtml";
    var eventPlaner = Session["eventplanner"] as EventPlanner;
    var loggedinuser = Session["myeventplanloggedinuser"] as AppUser;
}
@section links{
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="@Url.Action("Dashboard","Home")">My Dashboard</a>
        </li>
    </ol>
}

<div class="btn-controls">
    <div class="btn-box-row row-fluid">
        <div>
            <div class="btn-box-row row-fluid">
                    <div class="span8">
                        <div class="row-fluid">
                            <div class="span12">
                                <a href="@Url.Action("UserProfile", "Account")" class="btn-box medium span4">
                                    <i class="icon-user"></i><b>Profile Data</b>
                                </a>
                                <a href="@Url.Action("ChangePassword", "Account")" class="btn-box medium span4">
                                    <i class="icon-key"></i><b>Change Password</b>
                                </a><a href="@Url.Action("Index", "StaffEventMappings")" class="btn-box medium span4">
                                    <i class="icon-screenshot"></i><b>Background Color</b>
                                </a>
                            </div>
                        </div>

                    </div>
            </div>
        </div>
    </div>
    <!--/.content-->
</div>